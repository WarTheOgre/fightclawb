{
  "name": "fightclawb-tests",
  "version": "1.0.0",
  "description": "Test suite for FightClawb services",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 10000 --exit",
    "test:watch": "mocha tests/**/*.test.js --watch",
    "test:coverage": "nyc npm test"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.10",
    "supertest": "^6.3.3",
    "nyc": "^15.1.0"
  }
}
